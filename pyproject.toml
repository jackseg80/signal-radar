[project]
name = "signal-radar"
version = "4.0.0"
description = "Daily US stock signal research, next-open validation, and decision-support dashboard"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "pandas>=2.2",
    "yfinance>=0.2.40",
    "pyarrow>=15.0",
    "pyyaml>=6.0",
    "loguru>=0.7",
    "scipy>=1.12",
    "exchange_calendars==4.13.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
analysis = [
    "matplotlib>=3.8",
]
api = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
