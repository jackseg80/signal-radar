services:
  scanner:
    build: .
    env_file: .env
    environment:
      - TZ=${TZ:-Europe/Zurich}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config:ro
    restart: unless-stopped
